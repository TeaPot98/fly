// import { useContext } from 'react';
// import { context } from '../state/context';
import SearchForm from './SearchForm';

const Home = () => {
  // const { } = useContext(context);
  
  return (
    <>
      <SearchForm />
      {/* <div>{JSON.stringify(flyRoutes)}</div> */}
    </>
  );
};

export default Home;